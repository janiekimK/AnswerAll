<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Answer All" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=5,user-scalable=yes">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- Top navigation bar -->
    <div class="top-nav">
      <div class="nav-content">
        <div class="nav-left">
          <%= link_to "Answer All", root_path, class: "logo" %>
        </div>
        <div class="nav-right">
          <% if logged_in? %>
            <span class="user-info">Eingeloggt als <strong><%= current_user.name %></strong></span>
            <%= link_to "Dashboard", dashboard_path, class: "button nav-button" %>
            <%= button_to "Logout", session_path, method: :delete, form: { data: { turbo_confirm: "Sicher abmelden?" }, class: "nav-button-form" }, class: "button nav-button" %>
          <% else %>
            <%= link_to "Login", new_session_path, class: "button nav-button" %>
            <%= link_to "Registrieren", new_user_path, class: "button nav-button" %>
          <% end %>
        </div>
      </div>
    </div>

    <% if flash[:notice] %>
      <div class="flash notice"><%= flash[:notice] %></div>
    <% end %>
    <% if flash[:alert] %>
      <div class="flash alert"><%= flash[:alert] %></div>
    <% end %>

    <%= yield %>

  </body>
</html>
